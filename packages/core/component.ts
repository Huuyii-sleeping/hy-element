import { hyButton, hyIcon, hyButtonGroup, hyCollapse, hyCollapseItem, hyAlert, hyTooltip } from "@hy-element/components";
import type { Plugin } from 'vue'

export default [
    hyButton,
    hyIcon,
    hyButtonGroup,
    hyCollapse,
    hyCollapseItem,
    hyAlert,
    hyTooltip,
] as Plugin[]